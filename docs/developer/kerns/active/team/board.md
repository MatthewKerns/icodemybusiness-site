# Team board — icodemybusiness-site — run started 2026-09-04 09:51 PDT

_Owned this run by `cmo`._

## 0. Objectives

### 0.1 Directive
> **`/team cmo` — seat a marketing lead for this repo; write the cmo role card first, land the team protocol here.** (Matthew, 2026-09-04, via the `/team` skill and its two follow-up answers.)
> Standing decisions carried in: no visible prices, email capture + booked calls as the conversion (memory `project_offer_model_no_pricing`, 2026-09-02).
> Source now `docs/OFFER.md` on main @ `00dca22` (offer, 09-04; paper plan photo `docs/offer-paper-plan-2026-09.jpg`). Originally REPORTED via `offer` 10:20: selling "peace of mind" and "show up to work prepared with extra training"; ladder free training → paid consulting → done-for-you; capacity paid consulting 3 slots/week, done-for-you 2 slots/month; lead flow warm network (~20–30 people) + inbound from the four content pillars. OFFER.md open decisions (his): #2 fractional wording, #3 consulting path, #4 three tiers vs four paths; #1 program capacity resolved as "A few slots available per year" (`da28c3c`).

### 0.3 Engagement objectives
| Objective | Metric | Source | Status this run |
|---|---|---|---|
| Visitors reach a booked call | splash → assessment → `/book` funnel completion | PostHog 206048 (EU), dashboard 761841 | measured 09-04: 0 real visitors (prod = placeholder); staging 30d: 65 views / 3 assessments / 1 lead / 1 book click / 0 bookings captured |
| Copy carries no unsourced business claims | 0 unsourced claims on live surfaces | `docs/copy-principles.md` §2 scan | not yet scanned this run |

## Roster
| name [ref] | session_id (uuid) | role | tier | worktree | status | since |
|---|---|---|---|---|---|---|
| cmo [144b81] (pending `/rename`; currently `icodemybusiness-site-86`) | f9ec1669-ade1-4050-a37d-6b0989a1b017 | cmo | fable | .worktrees/team-cmo | active | 09-04 |
| offer [a878a0] | 1c021960-799f-46f1-a2f7-88c22654d9ee | peer (owns homepage letter) | — | ../icmb-wt-offer | live, not on team | 09-04 |
| email-followup [9c72de] | 999a964d-7db5-4e1d-9fd0-509f227b3ac1 | peer | — | shared checkout | live, not on team | 09-04 |
| business-intake [ee4b6f] | ebcf9937-75cc-42f9-8140-6ec9b5c9cdf5 | peer (owns intake flow) | — | shared checkout | live, not on team | 09-04 |

## Tasks
| id | title | serves | owner | tier | worktree / branch | PR | done-predicate | state | last update | next action | human gate? |
|---|---|---|---|---|---|---|---|---|---|---|---|
| M1 | Land the team protocol + cmo role card | directive | cmo | fable | team-cmo / agent/cmo/team-protocol | — | `.claude/team/{PROTOCOL.md,roles/*.md,board.template.md}` committed on the branch | review | 09-04 10:05 | Matthew reviews the cmo card; push + merge are his | YES (push/merge) |
| M2 | Baseline the funnel numbers | R-011 | cmo | fable | team-cmo | — | `docs/marketing/funnel-baseline.md` with `{ref, tree, command, result}` per step from PostHog 206048 | **done** — 65 pageviews/30d, all staging; 1 lead, 1 book click, recap reached once ever | 09-04 11:05 | re-run after cutover (R-002) and after `ac20cfb` deploys | none |
| M3 | Claim scan of live surfaces — audit at `docs/marketing/copy-trust-gap-audit.md`; C7/C8 sent to email-followup for story-intake | copy-principles §2 | cmo | fable | team-cmo | — | list of every number/duration/"every"/"guarantee" on `/`, `/consulting`, `/book`, `/free-tools` with source or `[CLAIM NEEDED]` | **done** | 09-04 12:05 | grep `src/content` + route files; coordinate with `offer` before touching the letter. Authoritative claims inventory is `docs/matthew-story-intake.md` on `agent/nurture/email-sequence` (email-followup) — groups C1–C6 cover landing.ts :67 :134 :145 :154 :155 :195 and academy :95 :229 :238; M3 appends to it, never a second list. Known-unverified already with `offer` (do not re-flag): `PATHS.fractional.commitment` "I hold very few of these at once", `PATHS.program.commitment` "a handful a year" — both agent-authored; second contradicts paper (2/month) | none (edits to the letter need `offer`) |
| M4 | Blog route decision brief | R-013 | cmo | fable | team-cmo | — | one-page brief: what the first 4 posts are (from `content/tracker.md`), what the route needs, sized for `sdm`/`dev-N` | queued | 09-04 | read `content/tracker.md` + `content/calendar.md` | none |
| M5 | Email follow-up copy review | R-010 / emailSends | cmo | fable | team-cmo | — | every template in `convex/emails.ts` + `/api/email/*` passes copy-principles §1–§3 | blocked | 09-04 10:35 | wait for `email-followup` ping that `convex/emails.ts` has settled (their approved nurture build touches it) | none (sends are Matthew's) |
| M6 | Opt-in audit — answer "are we missing opt-ins?" | directive (Matthew 09-04) | cmo | fable | team-cmo | — | `docs/marketing/opt-in-audit.md` with every capture surface, consent state, and ranked gaps | review | 09-04 10:40 | Matthew reads it; decides D3 | YES (D3) |
| M8 | `/consulting` says 15-minute (was 30-minute ×4) | R-001 / copy-principles §3 | cmo | fable | team-cmo / agent/cmo/team-protocol | — | 0 hits for `30-minute` on the consulting surface; gates green at head | review — gates green `14f547e` (264 tests) and `c7e051f` (272) | 09-04 13:10 | Matthew merges | YES (merge) |
| M9 | Consent line on every capture form (EmailCapture.tsx ×4 surfaces; DiscoveryRecap.tsx is business-intake's) | D3 (Matthew's ruling, REPORTED via email-followup 09-04) | cmo (copy) | fable | team-cmo | — | one line per form stating what emails follow and how to stop; no cadence word until F1 is answered | blocked(HUMAN: story-intake F1 cadence) | 09-04 12:05 | wait for F1 | YES (F1) |
| M10 | Funnel map + constraint identifier: `/admin/funnel` (owner-gated; vision + Convex-mirror constraint identifier) and PostHog dashboard 933266 (Key constraint SQL tile, 7-step funnel, views by host) | directive (Matthew 09-04 `/goal`) | cmo | fable | team-cmo / agent/cmo/team-protocol | — | page renders for an owner login on staging; PostHog tile names the constraint with numbers; gates green at head | **on staging** (d503b12 deployed; `/admin/funnel` → 307 to sign-in = owner gate live; VERIFIED 16:05). Defect found in the same check: the sign-in redirect_url is `https://0.0.0.0:3000/admin/funnel` (container-internal origin, not the public host) → M22 | 09-04 16:05 | M22 fix; Matthew signs in on staging to see the map | YES (his login) |
| M17 | **Pre-cutover security (Matthew's order, 14:15): agent session binding + `agentSessions` access control.** Today every function except `adminListSessions` branches only on row existence: holding a `da_…` id (Math.random, not crypto) grants full read/write of anyone's transcript and extracted answers. Fix: per-request authenticated Convex client on the four agent routes (never the module singleton), ownership model (unowned = id is the capability; owned = identity must match) | launch risk (VERIFIED by business-intake) | business-intake | — | ../icmb-wt-intake (new branch off d503b12) | — | routes authenticated per request; ownership guards; tests; gates; `ready to deploy:` | review — `ready to deploy: 206383e` (agent/intake/session-auth; 322 tests / 27 files; Convex schema + index → **Convex push must go first**) relayed to deploy 15:50 | 09-04 15:50 | deploy: Convex push, confirm index, then app; Matthew merges; 5 human checks (anonymous assessment first) | YES (merge; human checks) |
| M11 | **Second (Matthew 14:15; was first at 13:45)** Discovery chat route: per-session rate limit + upstream timeout (`src/app/api/agent/discovery/chat/route.ts` has neither; only the final submit is limited 3/hr) | launch risk #3 / Matthew's directive | business-intake | — | ../icmb-wt-intake | — | each turn rate-limited per session with a visitor-safe error; explicit Anthropic timeout → degrade(); tests; gates green; `ready to deploy:` to deploy | assigned | 09-04 13:50 | business-intake ACKs with plan + first check | none — Matthew authorised the route change (ask-first satisfied); merge is his |
| M12 | Booking recorded server-side: Calendly webhook → Convex row + server PostHog capture (today `consultation_booked` is a client-side iframe message only) | launch risk #7 / instrumentation #1 | queued (sdm / dev-N) | — | — | — | a booking made with the tab closed still appears in Convex and PostHog | queued | 09-04 13:40 | ask-first (new route) → then assign | YES (ask-first) |
| M13 | Academy promises review — every promise on `/academy` inventoried with OFFER.md labels (live / decided / being built / open); rows needing Matthew sent as questions | Matthew 13:55 ("assign this to 'offer'") | offer | — | ../icmb-wt-offer | — | **done by offer — `docs/academy-promises.md` on main @ `5ea5f83`** (20 rows, 10 rulings for Matthew; :95→row 11, :229→13, :238→17 match the artifact). one row per promise — line, words, label, evidence, question-or-true-wording; cross-checked against what exists (capture sends nothing, no unsubscribe, no outreach mechanism, no cohort record); questions to Matthew grouped; rows link to the review artifact's :95 :229 :238 so a ruling there maps here | review — file done, rulings pending | 09-04 15:05 | Matthew rules (artifact or file); offer executes | YES (his rulings) |
| M14 | Voice widget must not throw when Retell keys are unset; connects when set (R-004) | Matthew 13:58 ("lets have 'business-intake' fix this — I'll supply the retell keys") | business-intake (third, after M17 → M11; they will take Matthew's own word before starting) | — | ../icmb-wt-intake | — | widget does not mount when keys unset, no console errors; test; env var names to deploy; gates; `ready to deploy:` | assigned | 09-04 14:05 | business-intake ACKs; Matthew gives values to deploy | YES (key values → deploy) |
| M15 | Convex production deployment (R-012): find or create prod deployment + deploy key; promotion steps incl. the dev-data decision | Matthew 13:57 ("we should have prod convex keys somewhere I need to deploy") | deploy | — | — | — | existence answer (names only) + ordered promotion steps; then the promotion itself on a day Matthew picks | in-progress — ANSWERED 14:20 (deploy, VERIFIED via CLI): a prod deployment already exists (team matthew-17ee3, project icodemybusiness-site); prod env has 3 vars (Clerk issuer, Resend key, Resend from); no prod URL/deploy key in the VPS build env yet — everything still points at dev. Steps: (1) prod URL + deploy key from the Convex dashboard, (2) Matthew sets NEXT_PUBLIC_CONVEX_URL / CONVEX_DEPLOYMENT for prod in the VPS build env + the missing Convex env vars (diff pending), (3) data decision on 315 pageViews / 68 agentSessions in dev, (4) rebuild + redeploy | 09-04 14:20 | deploy diffs the env vars the app reads vs the 3 on prod; Matthew: keys + data decision | YES (keys, data decision) |
| M16 | Safe Browsing watch: `scripts/safe-browsing-check.sh` in cutover smoke + VPS cron (hourly week 1, then daily), paging on non-zero | Matthew 13:52 ("stay on top of it from the beginning") | cmo (script) → deploy (cron) | fable | team-cmo / agent/cmo/launch-readiness | — | script committed; cron installed by deploy; first-week log clean | in-progress | 09-04 14:05 | commit script; deploy installs cron | none (cron is deploy's) |
| M18 | Split-test mechanism + segment design: none exists today (only `?src=&v=` link attribution → cookies → `variant` on events/leads; no random assignment, no exposure event, no flag SDK use). Includes the paid-consulting rate test ($200–$500/hr greenlit by Matthew 15:35 via offer; the March rate-card tiers are retired and are not to be repeated anywhere). His rule: "costs are not surfaced in digital media — only tested / proposed on a call." So the test = (a) segment on the lead at capture (schema, ask-first) + (b) a post-call record of which rate was quoted (admin form on the lead/assessment) — no price variant in email or on any page | Matthew 14:30 ("critical to overcoming funnel constraints") | cmo (design) → sdm/dev-N (build) | fable | team-cmo | — | plan approved; registry + assignment + exposure event + admin `/admin/experiments`; PostHog experiment for stats | queued | 09-04 14:40 | answer Matthew; write the plan on his go | YES (schema field for segment; PostHog personal API key if admin controls flags) |
| M19 | One reply promise in Matthew's voice: `convex/emails.ts:162` and `src/emails/WelcomeEmail.tsx:99` say "a real human reads every message"; Matthew's current line (via email-followup 14:50, superseding "I read every reply."): "Reply to this email and I'll get back to you as soon as I can." — never with a turnaround time (§2). WelcomeEmail.tsx done on cmo branch; emails.ts:162 (+ :341 report template) wait for email-followup's file to settle (§7) — after email-followup's emails.ts settles; confirm RESEND_FROM_EMAIL on the Convex deployment doesn't override the matthew@ default (deploy, names-only) | copy-principles §7 / F3 answered | cmo | fable | team-cmo | — | one wording, three places; from-address confirmed | in-progress (1 of 3 done) | 09-04 14:55 | wait for emails.ts; ask deploy re from-address | none (his words already) |
| M20 | E-commerce follow-up email (`convex/emails.ts` sendEcommerceFollowupEmail): "We're putting together a tailored set of AI tools for your store and will follow up with next steps" — a deliverable + follow-up promise (§2/§3) in first-person plural beside Matthew's singular. Is it still true? | copy-principles §2 §3 | cmo → Matthew | fable | — | — | Matthew rules: keep (and it's built) / rewrite to what actually happens / delete | queued | 09-04 15:15 | add to the review artifact as item 21 | YES (his ruling) |
| M21 | Token budget raise on the discovery chat: `MAX_MESSAGE_CHARS = 2000` rejects the detailed answers the new frustration question draws out; `MAX_TOKENS = 700` truncates extraction JSON on rich answers (silent data loss). Make truncation observable too | Matthew (via business-intake 15:50) | business-intake (second, after M17) | — | ../icmb-wt-intake | — | limits raised with a stated basis; truncation visible in an event property; tests; gates | assigned | 09-04 15:50 | business-intake | none |
| M22 | `/admin/*` sign-in redirect carried the container bind address (`redirect_url=https://0.0.0.0:3000/...`) — owner login via the gate landed on a dead host | goal (owner-only dashboard) / R-016 | cmo | fable | team-cmo / agent/cmo/launch-readiness | — | redirect_url is the public URL; /forbidden redirect on the public host; unit tests | **done** — merged by deploy into main (`b4d60a9`) and VERIFIED on staging: signed-out `/admin/funnel` → `sign-in?redirect_url=https%3A%2F%2Fstaging.icodemybusiness.com%2Fadmin%2Ffunnel` | 09-04 16:20 | Matthew merges (middleware = ask-first); deploy ships; he signs in on staging | YES (merge) |
| M23 | Report email closing (`1dd83b9`, Matthew's third revision today): "our assessment / let us know / helps us" sits two inches above "I'll go through what you shared / I'll get back to you" — two voices in one email (§7). Is the split deliberate (assessment = system, call = him) or accidental? Also a recurring pattern to name for him: copy keeps reaching for a subscription relationship nobody has opted into ("as a thank you for opting in to the newsletter" was drafted and caught) | copy-principles §7 / D3 pattern | cmo → Matthew | fable | — | — | one ruling: one voice or a deliberate split | queued | 09-04 16:30 | put to Matthew with the next batch of rulings | YES (his ruling) |
| M25 | **done 09-06 — VERIFIED live: pk_live served, frontend host clerk.icodemybusiness.com, environment endpoint = iCodeMyBusiness-Site / production; IDEA Brand Coach gone.** Was: the apex authenticated against another product's Clerk instance. The publishable key baked into `icodemybusiness.com` decodes to `stunning-skunk-51.clerk.accounts.dev`; that instance's public environment endpoint returns `application_name: IDEA Brand Coach`, `instance_environment_type: development` (VERIFIED by cmo 09-05, independently of business-intake's report). Consequences: sign-in/sign-up on Matthew's site is branded as another product; `matthew@icodemybusiness.com` does not exist in that directory, so he gets "Couldn't find your account" and CANNOT reach `/admin/*`; the assessment's account gate sends visitors to another product's sign-up. Changing instances orphans every stored `clerkUserId` (users, leads, agentSessions, applications, assessments) | R-003 / launch risk #2, now live | Matthew (keys) → deploy (rebuild; NEXT_PUBLIC_* is baked at build time) | — | — | — | apex serves a `pk_live` (or at minimum an iCodeMyBusiness) Clerk instance; he can sign in; `/admin/funnel` opens | blocked(HUMAN) | 09-05 | business-intake is writing the change-over runbook; Matthew supplies keys | YES (keys + identity-migration decision) |
| M26 | **done 09-06 — VERIFIED end to end: Matthew re-signed-up as matthew@icodemybusiness.com; `users` row user_3Iy8BGY6s4n5hAUu1qxOmrtadLC carries server-derived `role: admin` (business-intake); he is inside /admin (13:20 screenshot, /admin/x with live Convex data). Domain rule did it — no OWNER_CLERK_USER_IDS, no per-user entry.** Was: no owner allowlist on the VPS: `.env.build` has 13 keys, none of them `OWNER_EMAIL_DOMAINS` / `OWNER_EMAILS` / `OWNER_CLERK_USER_IDS` (VERIFIED names-only 09-05). All three middleware routes fail closed, so every account → `/forbidden` even once M25 is fixed. Convex needs the same key or `requireOwner` throws | goal (owner dashboard) | Matthew (env) → deploy (rebuild) | — | — | — | `OWNER_EMAIL_DOMAINS=icodemybusiness.com` in VPS build env AND Convex env; owner reaches /admin/funnel | blocked(HUMAN) | 09-05 | set alongside M25's rebuild | YES (env) |
| M27 | **done — `f3e2a46`, live on the apex in `7216322`** (VERIFIED 09-05: apex 200, www 200, admin gate 307 to the public host). Root layout had no error boundary: `Providers.tsx` → `SessionBridge` runs `useEnsureUser` + `useBindDiscoverySession`, both calling Convex `useQuery`, which throws during render on error. Nothing sits between them and the root, so any Convex failure renders `global-error.tsx` over the whole site — the "A critical error occurred" screen Matthew screenshotted at 05:47 | live-site resilience | cmo | fable | team-cmo | — | a throwing query inside SessionBridge degrades that hook, children still render; test | queued | 09-05 | wrap in the existing `ErrorBoundary`; worth doing whatever the root cause | none |
| M28 | Owner refusal is invisible on client-side navigation: forbid() redirected only `Accept: text/html`; an RSC navigation (`RSC: 1` / text/x-component) got a bare JSON 403 rendered as the route error boundary. Fixed: `isNavigationRequest()` treats RSC navigations as navigations; fetch/API callers still get JSON 403 | live-site UX / today's lap | cmo (found by business-intake) | fable | team-cmo / agent/cmo/launch-readiness | — | signed-in non-owner lands on /forbidden after sign-in, not "Something went wrong"; unit tests; gates | **live** — merged by deploy (Matthew approved), DEPLOYED_SHA=da66437, verify-apex 5/5. Mechanism still inferred; verify by refusing the gmail account once and reading `[owner-gate] refused …` in the container log | 09-06 14:05 | optional verification after M29 | none |
| M29 | **LIVE: homepage dead for a signed-in visitor whose tab holds a session bound to another account.** `getForServer`/`getBySessionId`/`listMessages` THROW on a mismatched caller (session-auth 206383e); DiscoveryAssessment's useQuery throws during render → route error boundary on `/`. Matthew hit it at 13:46 after switching from the gmail account to matthew@icodemybusiness.com in the same tab. Blast radius: any account switch in one tab; any shared machine. Fix (business-intake, ETA ~30 min): reads return null/[] on mismatch, writes still throw, `getOrCreate` returns null, client mints a fresh session on null. Changes 3 test expectations → needs Matthew's OK in the commit body per AGENTS.md | funnel front door | business-intake | — | ../icmb-wt-intake | — | signed-in visitor with a foreign session id sees the homepage and a fresh assessment; tests updated with stated reason | **done — `926d293` on main and LIVE** (deploy: verify-apex 5/5; VERIFIED by cmo: getForServer/getBySessionId/listMessages return null via `mayUseSession`, client mints a fresh id; 136 insertions incl. tests). Matthew's tab: reload, no new tab needed | 09-06 14:35 | — | none |
| M30 | Skool community link: both "Join the Inner Circle" buttons (CommunityBanner, Footer) pointed at a guessed slug that 404'd live. Now `SKOOL_COMMUNITY_URL` = https://www.skool.com/icodemybusiness-9679 (Matthew, 14:10; VERIFIED 200) in one constant + test | Matthew's directive | cmo | fable | team-cmo / agent/cmo/launch-readiness | — | both buttons resolve to the real community | review — `6857a5a`, gates green (32 files / 343 tests), handed to deploy | 09-06 14:15 | Matthew merges (deploy relays his approval); deploy ships after M29 if simultaneous | YES (merge) |
| M7 | **applied by email-followup at `c4d3a7c` (their branch)** minus one clause: "tell you what I'd fix first" traces to the unratified 69a2ea7 letter and Matthew replaced it in the report today → story-intake C9 / artifact item 22 (also live at `landing.ts:204`). **Not a shipped fix for visitors:** this template only fires from the Retell webhook, dead in prod (R-004). Duplicate `style=` attribute bug fixed (real: `color:#999` never applied). Booking email copy: "30-minute discovery call" + "no pressure, just clarity" (`convex/emails.ts:200-206`); dead Calendly fallback handle in `convex/http.ts:237` | R-001 / copy-principles §5 | cmo (copy) + email-followup (http.ts fallback) | fable | team-cmo | — | template says Introduction Call, no duration hardcoded unless Matthew asserts it; fallback URL is the live handle or absent | queued | 09-04 | after M5 unblocks; ask email-followup to fix the fallback in their run since they own http.ts | none |

## Decision list
| # | decision | options | recommendation | evidence | raised | by |
|---|---|---|---|---|---|---|
| D8 | Clerk production keys (R-003) — **Matthew: "let's make sure I add the prod keys as this will block things"** (13:45). Done when a production instance bound to icodemybusiness.com exists, live keys are in the VPS build env, and a fresh build shows no dev-key console warning | Matthew adds keys → deploy rebuilds | do it in the same sitting as DNS (D7) | ROADMAP R-003 | 09-04 | cmo |
| D9 | Verify icodemybusiness.com in Google Search Console **before** cutover, so a Safe Browsing security issue notifies you and a review can be requested from inside GSC (the last flag, 13fa75c, was cleared via a GSC review request) | do it now / after cutover | now — it costs a DNS TXT or HTML file and nothing else | commit 13fa75c body; `scripts/safe-browsing-check.sh` reads clean for apex/www/staging 09-04 10:45 UTC | 09-04 | cmo |
| D10 | Rate-limit scope: business-intake VERIFIED that `ecommerce/chat`, `top3/chat`, `top3/complete` have no rate limit either (`grep -rn rateLimit src/app/api/agent/` → nothing). Same token bucket (40/h, cap 10) on all four, or only the discovery route? | all four / discovery only | all four, same ceiling | business-intake 09-04 14:00 | 09-04 | cmo |
| D11 | **Rotate the Resend API key.** `npx convex env list --prod` printed its value into the deploy session's output (REPORTED by deploy 14:20, flagged to Matthew directly; not repeated anywhere). The secret-guard hook covers file reads, not CLI output — process incident for AGENTS.md (deploy maintains it): boundary line + enforcement + engineering-log entry | rotate now / later | now; and never `env list` without `--json \| jq keys` or an equivalent names-only form | deploy message 09-04 14:20 | 09-04 | cmo |
| D12 | **VPS disk hit 100% (0 bytes free) during a gate run, 09-06 ~14:20** (deploy, REPORTED). Reclaimed 15 GB via docker-resource-manager with per-tier confirmation (stale 2.8 GB Playwright image + 14.4 GB build cache; nothing running, no volumes). Shared box, 30+ containers: this will recur. Options: a disk-usage alert (cron + threshold → Matthew), a scheduled `docker builder prune` with a size cap, or more disk. Not the cmo's lane beyond raising it | alert / scheduled prune / more disk | alert + capped weekly prune, deploy owns | deploy message 09-06 | 09-06 | cmo |
| D13 | **CORRECTED 14:55 — Matthew DID approve the M29 test change**, via an explicit confirmation in deploy's session ("Approve?" → "Yes, approve") before the merge. My earlier line ("no recorded OK") was wrong as stated: the OK existed; it was invisible — never relayed to the author who flagged it, never on the board, never in the commit. The real incident is recording, not permission. Enforcement adopted by deploy: `Test-Change-Approved: <who, date>` trailer required by pre-push for commits altering toThrow/rejects assertions in *.test.*; AGENTS.md line + ENGINEERING_LOG to follow | resolved | — | deploy 09-06 14:55 | 09-06 | cmo |
| D14 | **fixed** — marker now 9d15b03 (the deployed commit, includes 926d293); DEPLOY_LOG line appended. Still open: make deploy-staging.sh's recovery path write the marker itself (deploy, follow-up). Was: `/opt/icodemybusiness-site/DEPLOYED_SHA` read da66437 but the container (created 22:00:17Z) runs 926d293 — the manual ssh-hang recovery skipped the marker write. Anyone answering "what is live" from that file is wrong. deploy to correct it and make the recovery path write it | fix now | fix now, deploy | business-intake 09-06 (VERIFIED by them) | 09-06 | cmo |
| D7 | Launch: cut the apex over now (DNS at Namecheap + Clerk prod keys in one sitting, deploy runs `deploy.sh cutover`), holding the nurture sequence until consent lands | now / after R-003 only / after copy claims resolved | now, with R-003 in the same sitting; copy claims (C1–C8) are credibility not legal, fix after | `docs/marketing/launch-readiness-2026-09-04.md`; deploy session's read 09-04 | 09-04 | cmo |
| ~~D3~~ | ~~Add marketing consent to captures before the nurture sequence sends~~ — RESOLVED (REPORTED via email-followup 09-04): Matthew chose the strict option — consent line on every form + `leads.consentedAt`/`consentSource` first; every address captured before that is held out of sequences permanently. Capture on `/` + `/consulting` still open → D6 | — | — | email-followup message 09-04 ~11:50 | 09-04 | cmo |
| D5 | Move a one-line Measurable Progress Guarantee into the `/consulting` hero (the audit's trigger fix). It is a promise; repositioning duplicates it on the page | move / duplicate one line / leave | one line in the hero, full text stays | `copy-trust-gap-audit.md` §/consulting | 09-04 | cmo |
| D6 | Splash trigger + write-up line above the fold — **proposed, awaiting Matthew** (offer will raise it). Both reverse choices he made 09-04: he picked the splash lines himself ("I like B the most") and cut the gate paragraph to the heading ("just the title please") | yes / no / later | later; his call | audit ranked #4; offer 09-04 12:15 | 09-04 | cmo |
| D4 | `/services` subtitle "I take on a limited number of projects at a time" — agent-authored capacity claim (§2). Keep with real numbers (3/wk, 2/mo per paper plan via offer) / delete | keep w/ numbers / delete | keep with Matthew's numbers once OFFER.md lands | `src/app/services/page.tsx:240` | 09-04 | cmo |
| D1 | Where team worktrees live: skill says `.worktrees/<slug>` (mango board discovery finds these), AGENTS.md says `../icmb-wt-<you>` (a sibling checkout mango reads as a separate repo) | `.worktrees/` for team roles / sibling dirs for everyone / update AGENTS.md | `.worktrees/` for team roles, note it in AGENTS.md | `.gitignore:64` ignores `.worktrees/`; `mango…/team_board/reader.py` `_WORKTREE_CONTAINERS` | 09-04 | cmo |
| D2 | Is `cmo` a permanent role in the `/team` skill (add to `role ∈ …` in `~/.claude/skills/team/SKILL.md`) or a per-repo extension only? | global skill / repo-only | global — the skill's role list is the only place it is not yet named | this board, `roles/cmo.md` | 09-04 | cmo |

## Digest log
- 09-06 15:05 — AGENTS.md now carries the `Test-Change-Approved: <who, date>` trailer rule with pre-push enforcement (deploy; ENGINEERING_LOG 09-06) — D13's structural fix is landed. Laptop CRIT (17.9 GB swap): the screenshot watcher was killed by the OS and stays off until memory recovers; Matthew reports screenshots directly.
- 09-06 14:55 — CORRECTION: Matthew approved the M29 test-expectation change in deploy's session before the merge; the gap was visibility, not permission. I stated "no OK" as fact on the board and to business-intake — wrong; fixed here. Deploy adopts the `Test-Change-Approved` trailer + pre-push enforcement. DEPLOYED_SHA corrected to 9d15b03. Note: my outbound peer messaging has failed 4× since my session ref changed; peers still reach me. Board is the record until it recovers.
- 09-06 14:40 — business-intake, straight: no OK from Matthew on the M29 test-assertion change; it shipped on deploy's merge (D13, process incident — structural gate proposed). DEPLOYED_SHA marker stale after the manual recovery (D14). Both to deploy; D13 ratify/object to Matthew.
- 09-06 14:35 — **M29 live** (`926d293`): a foreign-bound session id no longer kills the homepage — reads return null, client re-mints. Deploy shipped it first, then the Skool link (M30, 6857a5a) is next. Deploy also recovered a mid-deploy ssh hang manually (ENGINEERING_LOG 09-06, different cause from 09-02) and logged the 0-bytes-free disk incident (9d15b03).
- 09-06 14:15 — M30: Skool link fixed at Matthew's request (the previous slug 404'd on the live site — two dead Inner Circle buttons since launch). Gated, handed to deploy behind M29.
- 09-06 13:53 — OBSERVED (screenshot, not discussed): Matthew is setting up a Skool community "iCodeMyBusiness" (private, unlisted, skool.com/icodemybusiness-9679, support email matthew@icodemybusiness.com). If this is the "Join the Inner Circle" destination in the site footer and/or the academy's home, it changes what /academy can honestly promise (M13, academy-promises.md). Not assumed; ask him before any copy moves.
- 09-06 14:05 — deploy: da66437 live (container swapped, verify-apex 5/5). M28 forbid() fix is on the apex. M29 (session-auth read throw) remains the live homepage bug for account-switched tabs; business-intake fixing, deploy will ship it ahead of the rate-limit stack.
- 09-06 13:55 — M29 opened: homepage error boundary for Matthew signed in (13:46). Cause confirmed by business-intake at the deployed sha: session-auth reads throw for a session bound to another subject; his tab held the gmail-bound id. Nothing unmerged fixes it (main = da66437, all cmo work in; deploy mid-build). Workaround: new tab. Fix in progress on intake's branch; changes 3 test expectations (Matthew's OK required). Rate-limit merge queued behind it (stacks on token-budget + prompt-caching → three commits).
- 09-06 13:41 — **GOAL MET AND OBSERVED.** Matthew's screenshots: icodemybusiness.com/admin/funnel behind his @icodemybusiness.com login renders the constraint identifier (verdict `insufficient` — "Enough arrivals, but no step has enough entrants to name a constraint"; 176 page views / 32 events, 30d), the why-bullets, what moves it, the 8-step map with counts (arrive 176 · splash 3 · started 3 · answering 2 · recap 1 · email 0 · book click 1 · booked unmeasured), and the measurement-gaps list. Every transition is excluded as "instrumented inside the window" (events first recorded 09-02) — the rule refusing to blame a half-measured step; the 7d view will judge. Follow-up (mine, small): the arrived→splash row should say "views are not people" before the instrumented check; and once the window start passes 09-02 the exclusions lift on their own.
- 09-06 — **Owner access VERIFIED.** Matthew took the re-sign-up route (matthew@icodemybusiness.com); the new `users` row has server-derived `role: admin`; he is inside /admin with live data. No per-user config anywhere → no stale-entry risk. `users` now holds 3 rows, 1 real (two gmail orphans written off per 09-05) — anyone counting users from that table: it reads 3, means 1. Business-intake's RSC-403 mechanism (M28) is untested-not-falsified; fix handed to deploy with that caveat, log line is the certain win. Remaining for the goal: Matthew opens /admin/funnel and the constraint card renders — still never observed.
- 09-06 — **First owner sign-in: two faults, one mine.** (1) The `convex` JWT template I created via API lacked `aud: "convex"` — Convex matches issuer AND audience; console showed the provider was correct and the token still rejected → null identity → Unauthorized. My earlier "issuer baked at deploy time" hypothesis was WRONG (retracted with deploy); my first PATCH also silently failed. Fixed and VERIFIED: claims now aud + email + email_verified. (2) Matthew signed up as **icodemybusiness@gmail.com** — domain gmail.com — so OWNER_EMAIL_DOMAINS doesn't match (business-intake, from the fresh `users` row user_3IuRIhnP…): authenticated, recorded, not an owner → requireOwner Forbidden. Decision for Matthew: pin the subject (OWNER_CLERK_USER_IDS, Convex + VPS) or re-sign-up as matthew@icodemybusiness.com. CORRECTED (business-intake, verified at the deployed sha): the middleware did NOT pass him — it refused him, but the post-sign-in navigation is a client-side RSC fetch, and forbid() only redirected document loads, so the router rendered the bare JSON 403 as an error boundary. The running container DOES have OWNER_EMAIL_DOMAINS (exactly `icodemybusiness.com`); my earlier "no OWNER_* at all" described the build file before his script. deploy: functions pushed to neat-hamster-414; an accidental push to the dormant prod deployment silent-jellyfish-951 synced schema only (57 indexes, no data) — logged for Matthew to keep or delete.
- 09-06 — business-intake independently confirmed the rebuild (own decode + environment query): pk_live only; host clerk.icodemybusiness.com; iCodeMyBusiness-Site/production; `stunning-skunk-51` and "IDEA Brand Coach" appear 0× on the live site. Note for later: the new instance's hosted sign-in is https://accounts.icodemybusiness.com/sign-in (Clerk Account Portal, Clerk-styled) while the site renders its own in-app /sign-in — two surfaces; the gate uses the in-app one. Follow-up: add Matthew's new subject to OWNER_CLERK_USER_IDS after first sign-in.
- 09-06 — **Clerk decoupling SHIPPED.** Rebuild live: pk_live=1/pk_test=0, host clerk.icodemybusiness.com → iCodeMyBusiness-Site (production); verify-apex 5/5. R-003 closes with this. Sequence: Matthew added 5 CNAMEs → Clerk dns/ssl/mail complete → deploy pushed CLERK_JWT_ISSUER_DOMAIN + OWNER_EMAIL_DOMAINS to Convex (no stale OWNER_* keys) → Matthew's keys script (had been non-executable after a rewrite; fixed) → rebuild. R-010's single old-instance user row is written off deliberately (Matthew, 09-05). Remaining: Matthew signs up on the new instance as matthew@icodemybusiness.com and opens /admin/funnel — the one thing still never observed. Google OAuth on prod still needs his Google Cloud client (non-blocking). Follow-up: add his new Clerk subject to OWNER_CLERK_USER_IDS; `authorizedParties` in clerkMiddleware (ask-first).
- 09-06 — **Clerk production domain VERIFIED complete** (dns/ssl/mail all `complete` per `clerk deploy status`; on the wire: JWKS 200 over a valid Google Trust Services cert, kid = ins_3Iy5ufDk…, issuer https://clerk.icodemybusiness.com). Matthew added the 5 CNAMEs. Remaining before the rebuild: (a) deploy's Convex CLI auth is broken — Matthew runs `npx convex login` in deploy's terminal, then deploy pushes CLERK_JWT_ISSUER_DOMAIN + OWNER_EMAIL_DOMAINS; (b) the VPS keys script ran but changed nothing (counts unchanged, no leftover file) — output requested from Matthew. Google OAuth on prod: pending Matthew's Google Cloud client, non-blocking.
- 09-06 — business-intake: the admin gate resolves through THREE vars (OWNER_EMAILS / OWNER_EMAIL_DOMAINS / OWNER_CLERK_USER_IDS). (1) A stale OWNER_CLERK_USER_IDS from the old instance gives a bare "Forbidden", not the helpful message — VPS has no OWNER_* key at all (VERIFIED earlier, names-only), so no stale list there; deploy asked to confirm the same for Convex env. (2) **Matthew's real account on the old instance was a gmail address.** He MUST sign up on the new instance as matthew@icodemybusiness.com or the domain rule refuses him and /admin → /forbidden — the same symptom as yesterday. Belt and braces after first sign-in: add the new subject to OWNER_CLERK_USER_IDS.
- 09-06 — business-intake check-in (at Matthew's request): live site unchanged (pk_test, IDEA Brand Coach host) — correct, nothing deployed yet; runbook `docs/runbook-clerk-production-keys.md` at 856a5a3 on `agent/intake/clerk-runbook`. Answered: identity decision = written off (Matthew 09-05); account-gate branding is a conversion fault (M25); **D10 still unanswered**. Their four branches incl. the chat rate limit (7ff09bf) remain unmerged on a public site — raise with Matthew with D10.
- 09-06 — **Clerk decoupling, in progress.** New app iCodeMyBusiness-Site (`app_3IubkZlt90DYPwRyudt4akMfwBg`). Matthew first created production + the domain on the WRONG app (MCF TikTok Connector Dev, `app_3D0VbSjrx…`) — caught from his 12:29 screenshot URL, VERIFIED via Platform API; I removed the domain there (dry-run → DELETE, dmn_3Iy3zrT8…), left its instance alone. Production then created on the right app: `ins_3Iy5ufDkxTdAcliu2whg0fuqOJm`, domain icodemybusiness.com, 5 CNAMEs pending at Namecheap. Keys pulled via `clerk env pull --instance prod` to a mode-600 scratch file (1 pk_live, 1 sk_live, 0 pk_test; values never printed). **`convex` JWT template existed on NEITHER instance of the new app** (it lived on the IDEA Brand Coach app) — created on prod via API, id jtmp_3Iy69cNIO59c3zxRNWUAwnKVoD0, claims email + email_verified. Google OAuth is enabled on dev; prod needs Matthew's own Google Cloud OAuth client (pending, non-blocking — email code works). Issuer for Convex: https://clerk.icodemybusiness.com — handed to deploy with OWNER_EMAIL_DOMAINS; Convex push waits for DNS to verify.
- 09-05 — **Process change every session must know: there is no staging-only deploy any more.** deploy's incident writeup (`88e4eb2`, ENGINEERING_LOG 09-05): the first routine staging deploy after cutover recreated the container with only the staging Traefik router and dropped the apex + www routers — both 404'd for about a minute until they re-ran cutover. `deploy.sh staging()` now always carries all three hostnames. Consequence for this board: **any sha I hand to deploy goes live to the public apex immediately.** Copy hand-offs are no longer rehearsals — treat every `ready to deploy:` as a publish, and hold anything carrying an unratified claim until Matthew rules.
- 09-05 — M27 done: the white-screen fix is live (`f3e2a46` in `7216322`); verify-apex 5/5 green per deploy. M25 (wrong Clerk instance) and M26 (no owner allowlist) remain blocked on Matthew — he is working on them now.
- 09-05 — **R-002 closed; apex live — with three faults.** (1) M25: the live site's Clerk is IDEA Brand Coach's dev instance (VERIFIED by decoding the served `pk_test` → `stunning-skunk-51.clerk.accounts.dev`, then its public environment endpoint → `application_name: IDEA Brand Coach`). Matthew cannot sign in to his own admin. (2) M26: no owner allowlist in the VPS env at all. (3) M27: root layout has no error boundary, so a Convex failure white-screens the site — matches his 05:47 screenshots. Also: staging and prod are the SAME container/Convex/dataset, so `funnel-baseline.md`'s "this is staging traffic" framing was corrected rather than left to read as wrong.
- 09-04 18:00 — subhead: Matthew saw the verbatim resequence beside the current line and chose **keep as written**. Closed; not to be reopened. The "identify the key constraints" through-line (offer = assessment = admin dashboard) stays on the board as a candidate proof beat for R-020's mechanism slot — `offer` will cite it only if that item returns to them, and only with his words.
- 09-04 17:30 — loop tick. Apex still 185.199.x (GitHub Pages); constraint unchanged = TRAFFIC, and the cutover now waits on Matthew saying go to `deploy` DIRECTLY (deploy correctly declined a relayed authorisation for an irreversible action — right call, backed). New subhead live on staging; assessed as a decision trigger: §2-clean but the only reader-side clause is last of three sentences (47 words) → offered `offer` a verbatim REORDER of his own clauses, outcome first. Named the through-line: "identify the key constraints" is now what the offer, the assessment and the admin dashboard all literally do — a proof beat that needs no testimonial (R-014 blocks those).
- 09-04 17:10 — **Matthew is changing the apex DNS now** (answered: "I'll change DNS now — stand by"). cmo watching public DNS; deploy pre-positioned for the cutover sequence. Also landed: launch-readiness merged (`b4d60a9`, redirect fix VERIFIED on staging, Safe Browsing clean ×3); session-auth live, anonymous assessment PASS (browser-verified by deploy), its 4 auth checks need a human; M24 closed by Matthew's own subhead (`c603b5f`) — he kept the I/We mix deliberately, which answers half of M23.
- 09-04 16:45 — Matthew: the homepage subhead reads out of date on staging. VERIFIED it is not a deploy lag — staging matches `origin/main` exactly; the line is unchanged since `06b95e4` (09-02) and no newer version exists on any branch. The offer moved today, the letter didn't. → M24 to offer.
- 09-04 16:30 — email-followup: report closing revised again (`1dd83b9`): "The Next Step" + intro-call offer; "free consultation session" kept as Introduction Call (§7); a drafted "thank you for opting in to the newsletter" caught (no newsletter, no consent yet) — same D3 pattern. Two-voices question → M23. Their suite: 36/36 on the VPS; tsc blocked only by stale Convex codegen.
- 09-04 16:20 — M22 fixed at `003cf42` (`src/lib/public-url.ts` + middleware), gates green, hand-off to deploy pending Matthew's merge. Branch agent/cmo/launch-readiness now carries: launch-readiness doc, safe-browsing script, welcome-email reply line, the redirect fix.
- 09-04 16:05 — loop tick: d503b12 is on staging (`/admin/funnel` gated, `/consulting` says 15-Minute; Safe Browsing clean ×3). Bug: `/admin/*` sign-in redirect carries `redirect_url=https://0.0.0.0:3000/...` — the middleware builds it from `request.url`, which behind Traefik is the container origin. Owner login via the gate lands on a dead host. Opened M22.
- 09-04 15:50 — business-intake: `ready to deploy: 206383e` (session binding + agentSessions access control; schema + index → Convex first) relayed to deploy. Matthew's queue for them: M17 → M21 (token budget) → M11 (rate limit, now with a visible-throttle event).
- 09-04 15:35 — Matthew via offer: $200–$500/hr greenlit; old rate-card tiers retired, not to be repeated; prices only spoken on the call, never in digital media → M18 = segment on lead + post-call quoted-rate record. OFFER.md #8 updated (offer pushing).
- 09-04 15:30 — email-followup applied M7 (`c4d3a7c`) except the unratified "what I'd fix first" clause → C9 / artifact item 22 (live on the homepage closing CTA, `landing.ts:204`, never ratified; Matthew declined it in the report). Template fires only via Retell, so M7 changes nothing visible until R-004 keys land.
- 09-04 15:15 — email-followup: emails.ts settled at `4a491f0` (their branch); reply promise now one voice in that file; corrected my line refs (main:162 was the e-commerce follow-up, not the booking email). Booking confirmation (sendCalendlyBookingEmail) is M7, mine, still unfixed — diff going to them since the file lives on their branch. M20 opened.
- 09-04 15:05 — offer: on main — academy-promises `5ea5f83` (M13 file done), rate-test decision in OFFER.md `add7b7e` (#7 resolved, #8 open), inbox section `57cc9fb` + email-followup corrections `c2219d7`.
- 09-04 14:45 — email-followup: Matthew did the report copy pass himself (`dfaa57a`); F3 answered (replies reach him); from-address default fixed to matthew@; `.claude/skills/email-voice/SKILL.md` added and shipped via .gitignore allowlist — read before M5. M19 opened for the reply-promise alignment.
- 09-04 14:40 — Matthew: how to enable split testing / is there a mechanism / admin management — "critical to overcoming funnel constraints" (M18). offer: OFFER.md #7 resolved (paid consulting $200–$500/hr, split-tested by segment, never a page price), #8 open; `docs/academy-promises.md` written — 20 rows, 10 rulings, academy capture sends nothing and its success message promises mail that never comes.
- 09-04 14:25 — offer ACK M13 with plan (separate audit file, labels reconciled with the copy audit, rulings via the artifact). Their 57cc9fb is merging onto d503b12.
- 09-04 14:20 — deploy: Convex prod deployment exists; promotion = keys into the VPS build env + env diff + dev-data decision + redeploy (M15). Incident: a Resend key value reached the deploy session's transcript via CLI output — D11 rotate; enforcement gap in secret-guard (CLI output) for AGENTS.md. Staging deploy of d503b12 gate-checking.
- 09-04 14:15 — Matthew → business-intake directly: "we need to fix the chat session binding first, and then the rate limit is next priority." Queue is M17 → M11 → M14. deploy CORRECTED (first sha from intake is binding, not rate limit). Four-routes question (D10) still unanswered.
- 09-04 14:05 — Matthew (four directives): academy promises → offer (M13); voice widget → business-intake, he supplies Retell keys (M14); "we should have prod convex keys somewhere" → deploy asked (M15); Safe Browsing "stay on top from the beginning" → check script written, VERIFIED clean ×3 hosts, cron to deploy (M16), GSC verification D9. Review artifact published for him: https://claude.ai/code/artifact/8902bffd-8d56-4526-a329-a27b48f91450 (20 rulings, funnel order, ladder tags).
- 09-04 14:00 — deploy: merge on origin/main `d503b12` (offer + value-delivery + intake + team-cmo), combined gates 24 files / 294 tests; staging deploy of that sha next. business-intake ACK M11: token bucket 40/h cap 10, own branch first, asking Matthew directly (ask-first held), found 3 more unlimited routes (D10). offer: OFFER.md `57cc9fb` adds the value-in-inbox section + label discipline (live / decided / being built / open); R-022 = nurture sequence, paused.
- 09-04 13:50 — Matthew: he will add the Clerk prod keys (D8); business-intake to fix + deploy the chat-route spend guard with top priority (M11 assigned, ask-first satisfied). deploy told to expect both.
- 09-04 13:40 — cmo: Matthew asked how close to launch and the risks. Answer (plan approved): 2 decisions of his (Clerk prod keys, DNS timing) + ~15 min mechanical deploy; 10 risks ranked in `docs/marketing/launch-readiness-2026-09-04.md`; D7 raised; M11/M12 queued as the first post-launch engineering tickets. Continuing on `agent/cmo/launch-readiness` so `agent/cmo/team-protocol` stays at `bce734d` for deploy's gate.
- 09-04 13:10 — cmo: gates green at `c7e051f` on the VPS (lint, tsc, 272 tests). Branch `agent/cmo/team-protocol` is ready: team protocol, /consulting 15-min, /admin/funnel map + constraint identifier, generated types. Hand-off sent to `deploy` (blocked on Matthew's merge); PostHog dashboard 933266 is live now.
- 09-04 12:50 — cmo: M10 committed (`003db8b`, `341b8fa`): the map page shows the constraint identifier at the top (Convex mirror, same rule as PostHog), the vision below, links to dashboard 933266. Gates + codegen running on the VPS.
- 09-04 12:30 — Matthew (in session): the site admin page shows the funnel VISION and links to PostHog for the REALITY; and he wants a constraint identifier on the site map too. Built: PostHog dashboard 933266 (Key constraint SQL tile, 7-step funnel, views-by-host) + `/admin/funnel` (vision + Convex-mirror constraint identifier, owner-gated). Gates at `14f547e` (consulting 15-min) passed on the VPS.
- 09-04 12:20 — offer → cmo: `docs/OFFER.md` on main @ `00dca22`; D6 proposals are "later", to be raised with Matthew by offer.
- 09-04 12:05 — cmo: plan approved by Matthew (`~/.claude/plans/refactored-forging-otter.md`). Copy audit filed; `/consulting` 30→15 committed (M8); offer asked for the splash/above-fold moves (D6); C7/C8 handed to email-followup; business-intake's recap-confirm correction applied to baseline + assessment. Consent: Matthew ruled strict (REPORTED) — D3 resolved, M9 opened, blocked on F1.
- 09-04 11:50 — email-followup → cmo: Matthew's consent ruling (strict); pushback on the quotes framing line accepted (dropped the "first thing to correct" clause — it invited an action the email can't offer). Their groundwork is `18ed3fd` on agent/nurture/email-sequence.
- 09-04 11:15 — cmo: read email-followup's `docs/matthew-story-intake.md` (their branch). It is the single claims questionnaire for Matthew; M3/M5 will append, not compete. Two audits (funnel+instrumentation, copy trust gaps) running on Sonnet subagents after the Explore agent type 404'd four times.
- 09-04 11:05 — cmo: M2 done. VERIFIED the 30d numbers business-intake reported (identical). Zero traffic from icodemybusiness.com; prod is still the placeholder (R-002). Their `ac20cfb` (Q1 anchor + recap events) is a pending behaviour change — baseline labelled pre-change.
- 09-04 10:40 — cmo: opt-in audit written (M6). Four gaps: no capture on `/`,`/consulting`,`/book`; no consent on any form; no unsubscribe anywhere (academy promises one); no blog surface. D3/D4 raised.
- 09-04 10:30 — email-followup → cmo: `convex/emails.ts` is in their approved nurture build (unsubscribe slot, suppressions, schema, crons); M5 blocked until they ping. Handed over 3 copy defects: booking email (M7), academy "leave any time" (fixed by their Phase 2, not by copy), landing.ts:145 capacity line (offer's, already D-tracked as known-unverified). CORRECTION from them 10:45: the memory file `reference_calendly_booking_links` was already right (15-min `new-meeting-1`); only its MEMORY.md index line said 60 min, and they fixed that line.
- 09-04 10:20 — offer → cmo: ACK on ownership split. Two capacity lines in landing.ts are agent-authored and with Matthew for real numbers; offer has proposed `docs/OFFER.md` as the single offer source and will notify when it lands.
- 09-04 10:05 — cmo: protocol, 7 role cards, board template and this board written in `.worktrees/team-cmo`; not pushed (human gate). No `sdm`/`qam`/`cto` live in this project; `offer`, `email-followup`, `business-intake` are live peers outside the protocol.
- 09-04 09:55 — cmo: searched the whole workspace — no `PROTOCOL.md`, no role cards, no `board.md` exist anywhere on this machine; the only prior definition of the board format is mango's parser + test fixture.
